import React from 'react';
import styles from './Calendar.module.css';

const Calendar: React.FC = () => {
  return <div className={styles.calendar}>Calendar component</div>;
};

export default Calendar;
